# CDBL Leave Management — Onboarding

- Built with **Next.js 16 (Turbopack + Cache Components)**.

- Uses Prisma + MySQL for persistence.

- Three main roles: Employee, HR Admin, Super Admin.

- UI uses Tailwind + shadcn/ui patterns.

## Development commands

```bash
pnpm dev
pnpm build
pnpm lint
```

## MCP

Cursor connects to:

- `next-devtools` for live route + cache inspection
- `codex` for structured task execution (`/tasks/*.md`)

